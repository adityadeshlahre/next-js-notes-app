# 13 — Spec consolidation

Type: task
Status: open
Blocked by: 01, 02, 03, 04, 05, 06, 07, 08, 09, 10, 11, 12

## Question

Assemble every resolution into the final implementation spec.

When all blocking tickets close, produce:

- One implementation-ready spec document (e.g. `docs/spec.md` or `.scratch/notes-app/spec.md`) covering: schema DDL, API contract, UI layout, auth flow, validation rules, test plan, deployment config, a11y checklist, README outline.
- The deliverables execution plan (repo creation, Render deploy, test account seeding) — even though build happens outside this map, the spec must state how each ASSIGNMENT.md deliverable is met.
- Optionally a ticket breakdown (to-tickets) for the build session.

Resolved when the spec is written and the map's Destination is reached.
